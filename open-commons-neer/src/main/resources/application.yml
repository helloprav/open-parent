server:
  max1-http1-header1-size1: 10MB
  servlet:
    context-path: /neer
    #register-default-servlet: true
spring:
  banner:
    location: classpath:/Banner.txt
  cache:
    cache-names:
    - 
  profiles:
    group:
      dev:  dev-jdbc, dev-email, dev-actuator, dev-ofds-security, dev-caching, prod-pariksha
      prod: prod-jdbc, prod-email, prod-actuator, prod-ofds-security, prod-caching, prod-pariksha
